---
# Locale defaults mirror the build-time defaults in mkosi.conf so the
# installer doesn't force a reset on the user. The UI still lets them
# override before proceeding.
region: "Europe"
zone:   "Oslo"
localeGenPath: "/etc/locale.gen"
